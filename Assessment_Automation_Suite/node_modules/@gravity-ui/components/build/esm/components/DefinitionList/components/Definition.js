import React from 'react';
import { ClipboardButton } from '@gravity-ui/uikit';
import { b } from '../utils';
export function Definition({ copyText, content, copyPosition }) {
    const iconInside = copyPosition === 'inside';
    const definitionContent = content !== null && content !== void 0 ? content : '—';
    return copyText ? (React.createElement("div", { className: b('copy-container', { 'icon-inside': iconInside }) },
        React.createElement("span", null, definitionContent),
        React.createElement(ClipboardButton, { size: "s", text: copyText, className: b('copy-button'), view: iconInside ? 'raised' : 'flat-secondary' }))) : (definitionContent);
}

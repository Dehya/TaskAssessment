import React from 'react';
import { Text } from '@gravity-ui/uikit';
import { b } from '../utils';
export function GroupLabel({ label }) {
    return (React.createElement("div", { className: b('group-title') },
        React.createElement(Text, { variant: "subheader-1", color: "complementary" }, label)));
}

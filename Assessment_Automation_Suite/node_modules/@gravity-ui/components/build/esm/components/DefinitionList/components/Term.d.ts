import React from 'react';
import { DefinitionListDirection, DefinitionListSingleItem } from '../types';
export interface TermProps extends Pick<DefinitionListSingleItem, 'note' | 'name' | 'nameTitle' | 'multilineName'> {
    direction?: DefinitionListDirection;
}
export declare function Term({ note, name, nameTitle, multilineName, direction }: TermProps): React.JSX.Element;
